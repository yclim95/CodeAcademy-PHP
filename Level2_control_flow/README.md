# Level2_control_flow

## PHP Control Flow 

### 1. Comparison 

List of comparison operators:

`>` Greater than
`<` Less than
`<=` Less than or equal to
`>=` Greater than or equal to
`==` Equal to
`!=` Not equal to